schema {
    query: QueryType
}

type QueryType {
    character(id: String): Character
    characters: [Character]
    episodes: [Episode]
}

type Character {
    id: ID!
    firstName: String
    lastName: String
    family: Boolean
}

type Episode {
    id: ID!
    name: String
    season: Season
    number: Int
    numberTotal: Int
    characters: [Character]
}

enum Season {
    Season1,
    Season2,
    Season3,
    Season4,
    Season5,
    Season6,
    Season7,
    Season8,
    Season9
}